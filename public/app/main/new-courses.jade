.panel.panel-danger
  .panel-heading.text-center Opponents
  .panel-body
    .row(ng-repeat="course in courses")
      .col-md-3 {{course.published | date: 'MMM d y'}}
      .col-md-9
        a(href="/courses/{{course._id}}") {{course.title}}
